import java.util.Set;

interface Item {};

interface Library {
	int numItems ();

	Set getItems ( String col )
	requires collection ( col );

	void addCollection ( String col )
	ensures collection ( col );

	void addItem ( Item i , String col )
	requires collection ( col )
	ensures next ( numItems ()) >= numItems ();
}
